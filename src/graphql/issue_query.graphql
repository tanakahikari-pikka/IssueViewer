query GetRepositoryIssues($repoId: [ID!]!) {
  nodes(ids: $repoId) {
    ... on Repository {
      id
      name
      issues(first: 10) {
        edges {
          node {
            title
          }
        }
      }
    }
  }
}
